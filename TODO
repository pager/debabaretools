TODO list generated by admin/genTODO.sh

wannaBuild: find a way to build when the source package is already installed in the repository
wannaBuild: ignore packages in failedToBuild.$DISTRO (should be manually removed from that file)
wannaBuild: fix this thing!!! pdebuilder should be called
needsBuild: replace the usage of awk with cut where possible
needsBuild: do not guess the .dsc file always is $ACCEPTED/$S_PACKAGE.dsc; better read from .changes
backends/buildQueue/plaintext.sh: check for the existence of already built but not installed!
