TODO list generated by admin/genTODO.sh

wannaBuild: find a way to build when the source package is already installed in the repository
needsBuild: do not guess the .dsc file always is $ACCEPTED/${SOURCE}_${VERSION}.dsc; better read from .changes
backends/buildQueue/plaintext.sh: use a better way to find the path to the .dsc file in parsePackageEntry
